[package]
name = "reseam-manager-ffi"
version = "0.1.0"
edition = "2021"
description = "BoltFFI surface for Reseam manager applications"

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
boltffi        = { workspace = true }
reseam-library = { workspace = true }
reseam-patcher = { workspace = true }
serde          = { workspace = true }
serde_json     = { workspace = true }
hex            = { workspace = true }
