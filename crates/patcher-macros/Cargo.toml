[package]
name = "reseam-patcher-macros"
version = "0.1.0"
edition = "2021"
description = "Proc macros for authoring Reseam patches"
readme = "README.md"

[lib]
proc-macro = true

[dependencies]
quote = "1"
syn = { version = "2", features = ["full"] }
