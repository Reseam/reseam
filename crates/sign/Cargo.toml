[package]
name = "reseam-sign"
version = "0.1.0"
edition = "2021"
description = "APK signing support for Reseam with Android Signature Scheme v2 and v3"
readme = "README.md"

[dependencies]
ring      = { workspace = true }
thiserror = { workspace = true }
tracing   = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
zip      = { workspace = true }
