[package]
name = "reseam-apk"
version = "0.1.0"
edition = "2021"
description = "APK container support for Reseam: ZIP, AXML, resources, and DEX extraction"
readme = "README.md"

[dependencies]
reseam-dex = { workspace = true }
thiserror  = { workspace = true }
zip        = { workspace = true }
quick-xml  = { workspace = true }
rayon      = { workspace = true, optional = true }
memmap2    = { workspace = true, optional = true }
tracing    = { workspace = true }

[dev-dependencies]
pretty_assertions = { workspace = true }
tempfile          = { workspace = true }
criterion         = { workspace = true }
reseam-sign       = { workspace = true }

[features]
default = ["parallel"]
parallel = ["dep:rayon", "reseam-dex/parallel"]
mmap = ["dep:memmap2", "reseam-dex/mmap"]

[[bench]]
name = "apk_bench"
harness = false
